#include <stdio.h>

/**
 * main - This is a description
 * Return: 0
 */
int main(void)
{
	char h;

	for (h = 48; h <= 57; h++)
	{
		putchar(h);
	}
	for (h = 97; h <= 102; h++)
	{
		putchar(h);
	}
	putchar('\n');
	return (0);
}
