#include <stdio.h>
/**
 * main - This is a description
 * Return: 0
 */

int main(void)
	{
		char c;

		for (c = 'a'; c <= 'z'; c++)
		{
			if (c == 'q' || c == 'e')
			{
			}
			else
			{
				putchar(c);
			}}
			putchar('\n');
			return (0);
}
